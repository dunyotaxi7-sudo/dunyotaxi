fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0          # async driver used by the app
psycopg[binary]==3.1.19  # sync driver — Alembic migrations (settings.sync_database_url)
alembic==1.13.1
geoalchemy2==0.15.1
shapely==2.1.2           # in-memory point-in-polygon for the service-area gate
numpy>=2.0,<3            # shapely's ABI dep — pin so it doesn't drift to an incompatible major
pydantic==2.7.1
pydantic-settings==2.3.0
redis==5.0.4
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
email-validator==2.1.1
httpx==0.27.0            # also used for Expo push delivery

# dev / test
pytest==8.2.1
pytest-asyncio==0.23.7
fakeredis==2.23.2
